conditiona statemtns:
1.if
2.if else
3.elif
4.nested if

1.if condition:if condition is nothing one condition one action we check the condition if it is true the the statement has print
syntax
if cond:
    #stmnt
2.if else:if else is to check the given condition is true or false if it is true then one condition is executed otherwise another condition will be elxecuted
syntax:
if cond:
    #stmnt
else:
    #stmnt
3.elif:it is like if we have a different condition and different actions then the elif can be used 
syntax:
if cond:
    #stmnt
elif cond:
    #stmnt
elif cond:
    #stmnt
else(opt):
    #stmnt
4.nested if:if we have neseted condition we use the nested condition means condition inside a condition
if cond:
    if cond:
        #stmnt
    else:
        #stmnt
else:
    #stmnt